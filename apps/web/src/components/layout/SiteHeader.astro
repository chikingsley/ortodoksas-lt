---
import PublicationHeader from "./publication-header";
import type { SiteLocale } from "../../lib/publication";

interface Props {
  locale?: SiteLocale;
}

const { locale = "lt" } = Astro.props;
---

<PublicationHeader client:load currentPath={Astro.url.pathname} locale={locale} />
